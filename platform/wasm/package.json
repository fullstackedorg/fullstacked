{
    "scripts": {
        "build": "node build.js",
        "start": "npm run build -- dev && node dev.js",
        "deploy": "npm run build && npx wrangler pages deploy out/site --project-name"
    },
    "type": "module",
    "dependencies": {
        "@types/winbox": "^0.2.5",
        "mime-types": "^2.1.35",
        "winbox": "^0.2.82"
    }
}
